---
   - name: install apache2
     apt: 
        name: apache2
        state: present
   - name: configure
     template:
        src: apache.conf.j2
        dest: /etc/apache2/apache.conf
        owner: root
        group: root
        mode: 0644
     notify: Restart apache
